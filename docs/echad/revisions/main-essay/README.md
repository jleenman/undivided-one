# Main essay revision gate

Add one proposal per substantive claim change to `PROPOSALS.md`. `proposed` is not implementable; `approved` permits a separate implementation task; `implemented` requires successful build/content/NL-EN review. Never combine unrelated proposals silently.
