# Editorial Synthesis and Core Essay Guard

Invoke `$echad-research` explicitly in the new isolated worktree for `/Users/jleenman/GitHub/undivided-one`. Read governance and inspect relevant live NL/EN routes. Preflight open draft PRs from prior editorial runs; if one is open and cannot safely be updated, or GitHub state cannot be read, stop for human review.

Review only reports completed since the previous run. Consolidate evidence-backed, non-duplicative proposals. Guard both baselines: canonical NL source must not exceed 9,030 words and the eventual approved compression target is 5,870–6,773; public rendered target is 8,219–9,483. Update only `docs/echad/revisions/main-essay/PROPOSALS.md`, `docs/echad/CONTENT-ARCHITECTURE.md`, `PLANS.md`, queue/backlog records, and status/decisions—except that an individually `approved` proposal may be applied to canonical NL and its EN mirror when all dependencies are satisfied and the change remains one reviewable scope.

Run parity/word-count checks, `npm test`, `npm run typecheck`, and `npm run generate` for canonical changes. Create/update a scoped draft PR. Never approve a proposal yourself, merge, deploy, publish, or grow the main essay.
